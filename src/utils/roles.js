export const Roles = Object.freeze({
  ADMIN: "admin",
  DISTRIBUTOR: "distributor",
  SUPPORT: "support",
  CUSTOMER: "customer",
});

export const STAFF_ROLES = Object.freeze([
  Roles.ADMIN,
  Roles.DISTRIBUTOR,
  Roles.SUPPORT,
]);
